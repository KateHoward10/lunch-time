<nav class="bg-teal-500">
  <div class="flex items-center justify-between flex-wrap max-w-5xl m-auto p-6">
    <div class="flex items-center flex-shrink-0 text-white mr-6">
      <%= link_to "Lunch Time", root_path, class: "font-semibold text-xl tracking-tight" %>
    </div>
    <div class="block lg:hidden">
      <button id="menu_button" class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
        <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
      </button>
    </div>
    <div id="menu" class="hidden w-full flex-grow lg:flex lg:flex-row lg:items-center lg:justify-between lg:w-auto">
      <div class="text-sm">
        <%= link_to "Menus", menus_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
        <%= link_to "Orders", orders_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
        <% if current_user.try(:admin?) %>
          <%= link_to "Options", options_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
          <%= link_to "Users", users_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
        <% end %>
      </div>
      <% if logged_in? %>
        <%= link_to "Log out", logout_path, class: "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
      <% end %>
    </div>
  </div>
</nav>